package com.team7.mobile.common.constant;

/**
 * 行程状态枚举 — 行程在生命周期中的各个阶段
 * <p>
 * DRAFT → PLANNED (Agent 生成) → APPROVED (审批通过)
 * → IN_PROGRESS (出行中) → COMPLETED → (异常流程) CANCELLED
 */
// TODO: 定义行程状态：DRAFT, PLANNED, APPROVED, IN_PROGRESS, COMPLETED, CANCELLED
public enum TripStatus {
}
