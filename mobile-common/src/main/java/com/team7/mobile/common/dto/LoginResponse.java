package com.team7.mobile.common.dto;

/**
 * 登录响应 DTO — 认证成功后返回给前端的 Token 信息
 * <p>
 * 字段：accessToken (JWT 令牌), tokenType (固定 "Bearer"),
 * expiresIn (过期秒数), userId, username, role
 */
// TODO: 定义 Token 相关字段，用于前端保存登录状态

