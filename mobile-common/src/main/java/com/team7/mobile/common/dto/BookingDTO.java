package com.team7.mobile.common.dto;

/**
 * 预订 DTO — 航班或酒店的预订确认信息
 * <p>
 * 字段：bookingRef (预订编号), type (FLIGHT/HOTEL), referenceName (航班号/酒店名),
 * price, currency, status (CONFIRMED/PENDING/CANCELLED), bookedAt
 */
// TODO: 定义预订确认信息，用于 Agent 自动预订后返回结果

