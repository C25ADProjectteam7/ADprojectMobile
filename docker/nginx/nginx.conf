# ============================================================
# Nginx reverse proxy — HTTPS termination + static file serving
# Deploy to /etc/nginx/sites-available/adproject-mobile
# ============================================================

# TODO: configure server block
# - listen 443 ssl (HTTPS with Let's Encrypt cert)
# - location /api/ → proxy_pass http://127.0.0.1:8080
# - location /images/ → alias /var/www/adproject-mobile/images/
# - security headers: X-Content-Type-Options, X-Frame-Options, CSP
